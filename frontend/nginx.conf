server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Tolère de gros en-têtes/cookies (évite le 400 "Request Header Too Large"
    # quand le navigateur a accumulé beaucoup de cookies sur localhost)
    client_header_buffer_size 16k;
    large_client_header_buffers 4 32k;

    # Application React (SPA) : toute route inconnue retombe sur index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy des appels API vers le service backend (réseau docker-compose)
    location /api/ {
        proxy_pass http://backend:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Cache des assets statiques (hash dans le nom de fichier → cache long)
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
